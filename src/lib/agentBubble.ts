/**
 * Shared constants for ALL floating agent trigger bubbles.
 * Matches Prime floating button exactly — every agent uses these values.
 */

export const BUBBLE_SIZE = 56;
export const BUBBLE_BOTTOM = 'calc(80px + env(safe-area-inset-bottom, 0px))';
export const BUBBLE_RIGHT = 'calc(24px + env(safe-area-inset-right, 0px))';
export const BUBBLE_Z = 40;


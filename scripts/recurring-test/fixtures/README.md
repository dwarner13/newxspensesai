# Recurring Detection Fixtures

Local-only fixtures for recurring charge detection tests.

Do **not** commit real statements or sensitive data.

Optional fixture format (`.json`):

```json
{
  "tag_output": {
    "transactions": []
  }
}
```

If no fixture JSON files are provided, built-in sample cases are used.

